# MathModelAgent Local Runtime

Follow `AGENTS.md` as the authoritative repository instructions.

The project is Skills-first and local-only. The old FastAPI, Redis, WebSocket, frontend, A2A, E2B, OpenAlex-agent, and internal LLM-factory runtime has been removed. Keep new work inside the local knowledge, CLI, Skill, and test boundaries unless the user explicitly changes that architecture.
