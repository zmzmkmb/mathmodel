// 附录：核心代码。注意：main.typ 末尾的"附录"部分有：
//   = 附录               (一级标题)
//   == 文件列表         (二级标题) + 文件列表 table
//   == 代码             (二级标题) + #include(this file)
// 所以本文件**必须**自带"== 代码"二级标题，承接在文件列表之后。

== 代码

[在此处粘贴用于复现结果的关键代码片段，使用 raw 块或 listings 包。推荐只放关键片段（10-60 行）。]
